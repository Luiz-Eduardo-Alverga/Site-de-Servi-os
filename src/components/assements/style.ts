import { styled } from 'styled-components'

export const Assessments = styled.div`
  display: flex;
  align-items: center;
  gap: 1.5rem;
  margin: 1.5rem;

  div {
    display: flex;
    align-items: center;
    font-size: 1.5rem;
    gap: 0.3rem;
    cursor: pointer;

    span {
      font-size: 1.2rem;
    }
  }
`
